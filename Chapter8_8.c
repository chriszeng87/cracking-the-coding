#include <stdio.h>

/**
 * Write an algorithm to print all ways of arranging eight queens on a chess board so that none of
 * them share the same row, column or diagonal.
 */

//int queens[8][8] = {0};
//int row[8] = {-1};
//int col[8] = {-1};

void rangeQueens(int board[], int n) {

	for (int i = 0; i < 8; i++) {

	}
}
